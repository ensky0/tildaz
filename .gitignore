zig-out/
zig-cache/
.zig-cache/
.DS_Store
*.exe
*.obj
*.o
*.pdb
.claude/
build.zig.tmp.*

# `compare-terminals.sh --capture` 가 남기는 화면 캡처 (#381). 측정 확인용이라 저장소에 넣지 않음.
dist/stress/shots/

# stray empty file 자꾸 생성됨 — root cause 미파악, 일단 ignore.
-10.1194

# Local analysis worktree (Windows Terminal source mirror for #136/#137 — 임시)
.wt-src/

# vendored ConPTY runtime (MS MIT) — shipped alongside tildaz.exe.
# x64/ + arm64/ subdir 각각 arch native binary.
!vendor/conpty/**/OpenConsole.exe
!vendor/conpty/**/conpty.dll
